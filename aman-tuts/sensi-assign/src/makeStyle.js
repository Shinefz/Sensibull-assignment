import { makeStyles } from '@mui/material';

export const useStyles = makeStyles(() =>
    createStyles({
        codeTitle: {
            marginTop: '12px',
            marginLeft: '20px',
        },
}));
